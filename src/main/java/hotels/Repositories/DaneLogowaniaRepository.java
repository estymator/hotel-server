package hotels.Repositories;

import hotels.models.DaneLogowania;
import org.springframework.data.repository.CrudRepository;

public interface DaneLogowaniaRepository extends CrudRepository<DaneLogowania, Integer> {
}
